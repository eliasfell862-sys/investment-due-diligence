# 投研项目与证券项目双工作台实施计划

通过独立路由和页面组件建立两个清晰工作台。保留现有投研项目数据结构与 `/projects` 路由，只调整用户可见术语；证券工作台首版不接入投研项目仓储。

## 范围

- 包含：侧栏双入口、投研项目文案、`/securities` 路由、证券工作台空状态、导航与页面测试。
- 不包含：证券项目数据模型、新建表单、项目列表、分析引擎和持久化。

## 实施步骤

- [ ] RED：在 `AppShell.test.tsx` 中锁定“01 投研项目”“02 证券项目”及 `/`、`/securities` 链接。
- [ ] RED：在 `ProjectListPage.test.tsx` 中锁定“投研项目工作台”“新建投研项目”和投研项目状态文案。
- [ ] RED：创建 `SecuritiesWorkbenchPage.test.tsx`，锁定独立标题、说明、空状态，并证明不出现投研项目列表和新建投研项目操作。
- [ ] 运行聚焦测试，确认因新导航、文案和证券页面尚未实现而正确失败。
- [ ] GREEN：修改 `AppShell.tsx`，增加两个编号明确、可激活的独立导航入口。
- [ ] GREEN：修改 `ProjectListPage.tsx` 的首页关键文案，不改变 repository、数据表和项目详情路由。
- [ ] GREEN：创建 `SecuritiesWorkbenchPage.tsx`，并在 `router.tsx` 注册 `/securities`。
- [ ] REFACTOR：复用现有页面和空状态 CSS，不引入证券业务数据或无效按钮。
- [ ] 验证聚焦测试、项目页面回归、typecheck、lint、build 和 `git diff --check`。
- [ ] 仅提交本功能文件，避免混入当前工作区其他引擎和页面改动。

## 验收

- 左侧栏依次显示 `01 投研项目`、`02 证券项目`。
- 两个入口进入不同工作台，激活状态正确。
- 现有投研尽调功能和数据不受影响。
- 证券工作台不读取或复用投研项目数据。